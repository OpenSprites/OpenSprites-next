<header><main>
  <h1>{{collection.name}}</h1>
  {{{by collection.owners}}}
</main></header>

<main>
  <div {{#if collection.youOwn}}contenteditable{{/if}} class='bio'>
    {{{md collection.about}}}
  </div>
  <small></small>

  {{#if collection.youOwn}}
    <a id='edit' class='btn flat' style='float:right'>edit about me</a>
  {{/if}}

  <script>var bio_raw = '{{json collection.about}}'; var csrfToken = '{{json csrfToken}}';</script>

  <div class='resources'>
    {{#each resources}}
      {{> resource this}}
    {{/each}}
  </div>
</main>